bin/
obj/
.vs/
*.user

# Per-developer local overrides (may contain real connection strings/secrets)
appsettings.Development.json
appsettings.*.local.json

# Local always-on server logs (scripts/start-server.ps1)
.logs/
